# Vue Translation App Environment Variables
#
# LibreTranslate is used by default for privacy-friendly translations.
# Provide additional configuration below if you run your own instance or
# want to tweak model behaviour.

# Whisper Backend Configuration
#
# The transcription service loads the large-v3 model by default. Override the
# model name here if you need a smaller footprint (e.g. medium, small, base).
WHISPER_MODEL_SIZE=large-v3
# Controls how many worker threads faster-whisper uses during inference.
WHISPER_BATCH_SIZE=4
VITE_WHISPER_ENDPOINT=https://whisper.obiente.cloud/transcribe
# Optional WebSocket endpoint for streaming transcription mode.
VITE_WHISPER_STREAMING_ENDPOINT=wss://whisper.obiente.cloud/ws/transcribe

# Uncomment to persist incoming audio chunks for debugging.
# WHISPER_DEBUG_DIR=./.whisper-debug

# LibreTranslate Configuration
#
# Point to the LibreTranslate endpoint used for text translation, and control
# how many alternative translations are requested (1-10).
VITE_LIBRE_TRANSLATE_BASE=https://libretranslate.obiente.cloud
VITE_LIBRE_TRANSLATE_ALTERNATIVES=3